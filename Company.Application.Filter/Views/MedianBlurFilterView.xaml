<UserControl x:Class="Company.Application.Filter.Views.MedianBlurFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Company.Application.Filter.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:share="clr-namespace:Company.Application.Share.Prism;assembly=Company.Application.Share"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Slider Maximum="21" Minimum="3" Value="{Binding FilterModel.Value}" Width="100"/>
            <TextBox Height="25" Text="{Binding FilterModel.Value, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="50"/>
            <TextBlock Text="3,5,7..."/>
        </StackPanel>
    </Grid>
</UserControl>
