<UserControl x:Class="Company.Application.Filter.Views.GaussianBlurFilterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Company.Application.Filter.Views"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:share="clr-namespace:Company.Application.Share.Prism;assembly=Company.Application.Share"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Slider Maximum="9" Minimum="1" TickFrequency="2" IsSnapToTickEnabled="True" Value="{Binding FilterModel.Width}" Width="100"/>
            <TextBox Height="25" Text="{Binding FilterModel.Width, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="50"/>
            <TextBlock Text="Width值"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Slider Maximum="9" Minimum="1" TickFrequency="2" IsSnapToTickEnabled="True" Value="{Binding FilterModel.Height}" Width="100"/>
            <TextBox Height="25" Text="{Binding FilterModel.Height, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" Width="50"/>
            <TextBlock Text="Height值"/>
        </StackPanel>
    </StackPanel>
</UserControl>
