<UserControl
    x:Class="Company.Application.Main.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Company.Application.Main.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:share="clr-namespace:Company.Application.Share.Prism;assembly=Company.Application.Share"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Background="White"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="500" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <ContentControl prism:RegionManager.RegionName="{x:Static share:RegionNames.ImageRegion}" />
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" /> <!--菜单-->
                <RowDefinition Height="auto" /> <!--快捷按钮-->
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <ContentControl Grid.Row="0" prism:RegionManager.RegionName="{x:Static share:RegionNames.MenuRegion}" />
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Content="&#xf115;" Style="{StaticResource IconButtonStyle}"></Button>
                <Button Content="&#xf0c7;" Style="{StaticResource IconButtonStyle}" ToolTip="保存图像"></Button>
                <Button Content="&#xf044;" Style="{StaticResource IconButtonStyle}"></Button>
                <Button Content="&#xf293;" Style="{StaticResource IconButtonStyle}"></Button>
                <Button Content="&#xf240;" Style="{StaticResource IconButtonStyle}"></Button>
                <Button Content="&#xf030;" Style="{StaticResource IconButtonStyle}" ToolTip="拍照采集（相机+探测器）" Command="{Binding GrabPhotoCommand}"></Button>
                <Button Content="&#xf03e;" Style="{StaticResource IconButtonStyle}"></Button>
                <CheckBox x:Name="mouseWorkMode" Content="&#xf0b2;" Style="{StaticResource IconCheckBoxStyle}"></CheckBox>
                <ComboBox x:Name="comboBox" Height="25" MinWidth="70" IsEnabled="{Binding ElementName=mouseWorkMode, Path=IsChecked}"></ComboBox>
            </StackPanel>
            <ContentControl Grid.Row="2" prism:RegionManager.RegionName="{x:Static share:RegionNames.PrimaryRegion}" />
            <ContentControl Grid.Row="3" prism:RegionManager.RegionName="{x:Static share:RegionNames.AxisRegion}" />
            <ContentControl Grid.Row="4" prism:RegionManager.RegionName="{x:Static share:RegionNames.CameraRegion}" />
        </Grid>
    </Grid>
</UserControl>
